@using QuizzesApp.Models
@model QuizOverview;
@using QuizzesApp.Components

@{
    ViewData["Title"] = "Questions";
}

<section>
    <img src="@Model.IconPath" alt="@Model.Title FEM icon." />
    <h1>@Model.Title</h1>
    <component type="typeof(QuizzesApp.Components.Quiz)" render-mode="ServerPrerendered" ref="" />
</section>
